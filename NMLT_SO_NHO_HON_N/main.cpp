#include <iostream>
#include<cmath>
using namespace std;
int main() {
    int n;
    float x;
    do
        cin>>n;

    while(n>2);
        for (int i = 2; i <= sqrt((float)x); i ++)
            do
                cin>>n;

            while(x<=sqrt(n));
        cin>>x;
    }
